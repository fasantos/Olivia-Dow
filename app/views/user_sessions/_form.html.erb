<div class="grid_4 alpha omega">
  <%= form_for(@user_session = UserSession.new) do |f| %>
    <% if @user_session.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@user_session.errors.count, "error") %> prohibited this user_session from being saved:</h2>

        <ul>
        <% @user_session.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="grid_2 alpha beta omega">
      <p>
        <%= f.label :username %>:
      </p>
      <div class="actions container grid_1">
      </div>
      <p>
        <%= f.label :password %>:
      </p>
    </div>
    <div class="grid_2 alpha beta gama ">
      <p>
        <%= f.text_field :username, class: "grid_2 alpha beta omega" %>
      </p>
      <div class="actions container grid_1">
      </div>
      <p>
        <%= f.password_field :password, class: "grid_2 alpha beta omega" %>
      </p>
    </div>
    <div class="grid_2 alpha beta omega"></div>
    <div class="actions grid_2 alpha beta gama">
      <%= f.submit 'Login', class: "grid_1 beta omega" %>
    </div>
  <% end %>  
</div>

